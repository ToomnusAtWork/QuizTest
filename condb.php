<?php
    $conn = new SQLite3('testing.sqlite') or die("Unable to open database!");
    // $query="CREATE TABLE IF NOT EXISTS `eadesign_romregion`(entity_id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, region_id TEXT, region TEXT, region_en TEXT)";
    // $conn->exec($query);
?>